import 'package:flutter/material.dart';

class MissingInformation extends StatelessWidget {
  const MissingInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Missing Information'),
      ),
    );
  }
}
